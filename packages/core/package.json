{
  "name": "@elvia/core",
  "version": "0.1.0",
  "private": true,
  "description": "Pure business logic shared across workspaces (no React, no Express)",
  "main": "src/index.js",
  "scripts": {
    "test": "node --test src/**/*.test.js"
  }
}
